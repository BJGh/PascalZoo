Program max_of_two;
var a, b: integer;
begin
writeln ('¬ведите целое число a');
readln (a);
writeln ('¬ведите целое число b');
readln (b);
if a > b then
writeln (a, 'больше чем' , b)
else
writeln (b, 'больше чем' , a);
end.